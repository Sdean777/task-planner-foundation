{
  "patchedJob": "push-image-placeholder",
  "patchVersion": "v1",
  "activationGuard": [
    "github.ref == refs/heads/main",
    "inputs.target_environment == production",
    "inputs.image_tag starts with sha-",
    "inputs.image_tag != sha-<git-sha>",
    "vars.AWS_ACCOUNT_ID is present",
    "vars.AWS_REGION is present"
  ],
  "jobLocalPermissions": [
    "contents: read",
    "id-token: write"
  ],
  "addedFirstJobBehavior": [
    "checkout source for build context",
    "show bounded activation posture",
    "derive bounded ECR image coordinates",
    "configure GitHub OIDC AWS credentials",
    "log in to ECR",
    "build bounded foundation image",
    "push immutable sha image tag first",
    "push main convenience mirror second"
  ],
  "preservedLines": [
    "push-image-placeholder",
    "needs: activation-gate-summary",
    "workflow-level permissions: contents: read",
    "workflow_dispatch-only trigger",
    "ecs-rollout-placeholder still disabled",
    "public-verification-placeholder still disabled"
  ],
  "stillForbiddenActions": [
    "Enable ecs-rollout-placeholder",
    "Enable public-verification-placeholder",
    "Widen workflow-level permissions",
    "Add static AWS credentials",
    "Change app.py"
  ],
  "outcome": [
    "live_enable_patch_applied",
    "live_enable_patch_first_job_only",
    "live_enable_patch_later_jobs_still_disabled"
  ],
  "notes": [
    "Planning artifact only.",
    "Records the first bounded live-enable workflow patch.",
    "Does not authorize later-stage deployment widening."
  ]
}
