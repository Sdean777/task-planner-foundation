{
  "patchedJob": "push-image-placeholder",
  "patchVersion": "v1",
  "preservedLines": [
    "push-image-placeholder",
    "if: ${{ false }}",
    "needs: activation-gate-summary",
    "workflow-level permissions: contents: read",
    "workflow_dispatch-only trigger"
  ],
  "addedPlaceholderMessages": [
    "Future image push placement: inside push-image-placeholder after OIDC auth and ECR login.",
    "Future push posture: publish immutable sha-<git-sha> image truth first.",
    "Future convenience tag posture: main may mirror sha tag but must not replace it.",
    "Future push target: explicit ECR registry and repository from approved non-secret boundaries.",
    "No live push, ECS rollout, public verification, or static credentials are allowed in this mission."
  ],
  "unchangedJobs": [
    "activation-gate-summary",
    "ecs-rollout-placeholder",
    "public-verification-placeholder"
  ],
  "stillForbiddenActions": [
    "Enable push-image-placeholder",
    "Change workflow-level permissions",
    "Add docker push",
    "Add ECS rollout behavior",
    "Add public verification behavior",
    "Add static AWS credentials"
  ],
  "outcome": [
    "push_placeholder_patch_applied",
    "push_placeholder_patch_still_disabled",
    "push_placeholder_patch_still_non_deploying"
  ],
  "notes": [
    "Planning artifact only.",
    "Records the first reviewed image-push placeholder hunk patch.",
    "Does not authorize live image publication."
  ]
}
