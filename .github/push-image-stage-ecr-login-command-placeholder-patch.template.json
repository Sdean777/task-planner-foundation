{
  "patchedJob": "push-image-placeholder",
  "patchVersion": "v1",
  "preservedLines": [
    "push-image-placeholder",
    "if: ${{ false }}",
    "needs: activation-gate-summary",
    "workflow-level permissions: contents: read",
    "workflow_dispatch-only trigger"
  ],
  "addedPlaceholderMessages": [
    "Future login-command placement: inside push-image-placeholder after the future auth-action step and before any future image-push step.",
    "Future login-command invocation: aws ecr get-login-password --region <approved-region> | docker login --username AWS --password-stdin <approved-ecr-registry>.",
    "Future explicit inputs: approved non-secret AWS region plus approved non-secret ECR registry URI only.",
    "No live auth action, login command, image push, ECS rollout, or static credentials are allowed in this mission."
  ],
  "unchangedJobs": [
    "activation-gate-summary",
    "ecs-rollout-placeholder",
    "public-verification-placeholder"
  ],
  "stillForbiddenActions": [
    "Enable push-image-placeholder",
    "Change workflow-level permissions",
    "Add uses: aws-actions/configure-aws-credentials@v4",
    "Add aws ecr get-login-password",
    "Add docker login",
    "Add docker push",
    "Add static AWS credentials"
  ],
  "outcome": [
    "login_command_placeholder_patch_applied",
    "login_command_placeholder_patch_still_disabled",
    "login_command_placeholder_patch_still_non_deploying"
  ],
  "notes": [
    "Planning artifact only.",
    "Records the first reviewed ECR-login command placeholder hunk patch.",
    "Does not authorize live registry login or image push."
  ]
}
