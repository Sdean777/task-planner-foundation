{
  "patchedJob": "push-image-placeholder",
  "patchVersion": "v1",
  "preservedLines": [
    "push-image-placeholder",
    "if: ${{ false }}",
    "needs: activation-gate-summary",
    "workflow-level permissions: contents: read",
    "workflow_dispatch-only trigger"
  ],
  "addedPlaceholderMessages": [
    "Future auth-action placement: inside push-image-placeholder only.",
    "Future auth-action invocation: aws-actions/configure-aws-credentials@v4.",
    "Future explicit inputs: role-to-assume, aws-region, role-session-name only.",
    "Future auth-action ordering: before ECR login and image push.",
    "No live auth action, permission change, login, push, or static credentials are allowed in this mission."
  ],
  "unchangedJobs": [
    "activation-gate-summary",
    "ecs-rollout-placeholder",
    "public-verification-placeholder"
  ],
  "stillForbiddenActions": [
    "Enable push-image-placeholder",
    "Add a uses: auth action step",
    "Add a job-level permissions block",
    "Change workflow-level permissions",
    "Add login commands",
    "Add push commands",
    "Add static AWS credentials"
  ],
  "outcome": [
    "auth_action_placeholder_patch_applied",
    "auth_action_placeholder_patch_still_disabled",
    "auth_action_placeholder_patch_still_non_deploying"
  ],
  "notes": [
    "Planning artifact only.",
    "Records the first reviewed auth-action placeholder hunk patch.",
    "Does not authorize live auth-action invocation."
  ]
}
