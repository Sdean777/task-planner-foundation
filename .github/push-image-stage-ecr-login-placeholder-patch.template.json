{
  "patchedJob": "push-image-placeholder",
  "patchVersion": "v1",
  "preservedLines": [
    "push-image-placeholder",
    "if: ${{ false }}",
    "needs: activation-gate-summary",
    "workflow-level permissions: contents: read",
    "workflow_dispatch-only trigger"
  ],
  "addedPlaceholderMessages": [
    "Future ECR login placement: inside push-image-placeholder after OIDC auth and before image push.",
    "Future login shape: aws ecr get-login-password piped to docker login --password-stdin.",
    "Future login inputs: explicit AWS region and explicit ECR registry URI from approved non-secret boundaries.",
    "Future rollout identity still requires immutable sha-<git-sha> image truth.",
    "No live login, image push, ECS rollout, or static credentials are allowed in this mission."
  ],
  "unchangedJobs": [
    "activation-gate-summary",
    "ecs-rollout-placeholder",
    "public-verification-placeholder"
  ],
  "stillForbiddenActions": [
    "Enable push-image-placeholder",
    "Change workflow-level permissions",
    "Add aws ecr get-login-password",
    "Add docker login",
    "Add docker push",
    "Add static AWS credentials"
  ],
  "outcome": [
    "login_placeholder_patch_applied",
    "login_placeholder_patch_still_disabled",
    "login_placeholder_patch_still_non_deploying"
  ],
  "notes": [
    "Planning artifact only.",
    "Records the first reviewed ECR-login placeholder hunk patch.",
    "Does not authorize live registry login."
  ]
}
